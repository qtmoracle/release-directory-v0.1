Directory v0.3 — LOCAL FREEZE ONLY
Status: NOT published to IPFS

Notes:
- This directory supersedes release_directory_v0.2 locally.
- Brick A (Idempotency) + Brick B (Listing Toggle) are included and locked.
- No IPFS root CID has been generated yet.
- Do not treat this directory as immutable until explicitly published.
